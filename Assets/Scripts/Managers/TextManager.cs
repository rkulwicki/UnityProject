using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TextManager : MonoBehaviour, IManager
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //todo!!
    public void SetText()
    {

    }

    //replace some of the functionality of TextBoxScript and put it into here
    //so that way TextBoxScript can focus on displaying the text and reading
    //the text.
    //The goal of this script is to be able to generate the text boxes and 
    //give them their text, just like a manager should do.
    //The text box trigger should just tell this manager to generate a 
    //text box with the following text. (and later optional functionality)

}
